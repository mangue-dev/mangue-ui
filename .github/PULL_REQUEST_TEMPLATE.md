## Summary

What does this PR change? Link related issues (`Fixes #123`).

## Changes

- …

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] Showcase updated with an example (new component / behavior change)
- [ ] README updated if the public API changed
- [ ] Version bumped in `packages/mangue-ui/package.json` if warranted
